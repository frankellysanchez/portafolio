export type ProjectData = {
    title: string;
    description: string;
    icon?: string; 
    thumb?: string;
    features?: string[];
    details?: string;
    tags?: string[];
    gallery?: { img: string; caption: string }[];
  };
  